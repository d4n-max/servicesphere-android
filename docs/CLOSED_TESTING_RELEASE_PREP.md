# Closed Testing Release Prep

Last updated: 2026-06-05

## App Identity
- App name: ServiceSphere
- Version name: 1.0.0
- Version code: 2
- Application ID: com.servicesphere.app
- App label: ServiceSphere
- Launcher icon: `@mipmap/ic_launcher`
- Round icon: `@mipmap/ic_launcher_round`

## Build Status
- `clean`: Passed on 2026-06-05 after stopping a stale Gradle daemon that held a Windows file lock.
- `assembleDebug`: Passed on 2026-06-05.
- `lintDebug`: Passed on 2026-06-05.
- AAB status: AAB not generated by Codex. Generate manually in Android Studio: Build > Generate Signed App Bundle / APK > Android App Bundle.

## Release Config Status
- Release build type is explicit with `debuggable=false`, `minifyEnabled=false`, and `shrinkResources=false`.
- No committed release signing config, keystore, passwords, or private signing values were added.
- `versionName` is `1.0.0`.
- `versionCode` is `2`.
- Every new upload to Google Play must use a higher versionCode than the previous uploaded bundle.

## Permissions
- `CAMERA`: photo proof capture.
- `INTERNET`: RevenueCat/subscription support when configured.
- `POST_NOTIFICATIONS`: job reminder notifications.
- No broad storage permissions are declared.

## RevenueCat Status
- Public SDK key is expected from `REVENUECAT_API_KEY` in `local.properties` or a private Gradle property.
- Missing or blank `REVENUECAT_API_KEY` does not crash the app.
- Missing or blank `REVENUECAT_API_KEY` leaves the app on the Free plan and the paywall shows a safe not-configured message.
- Expected entitlement: `pro`.
- Expected product IDs: `servicesphere_pro_monthly`, `servicesphere_pro_yearly`.

## Keystore Note
Keystore should be stored outside repo, e.g. C:\AndroidKeys\ServiceSphere\servicesphere-release-key.jks

## Manual Next Steps in Android Studio
1. Build > Generate Signed App Bundle / APK
2. Choose Android App Bundle
3. Select module app
4. Choose/create keystore outside repo
5. Build release
6. Locate generated AAB
7. Upload to Play Console Closed Testing
